import { Box } from '@mui/material';

import useStyles from './FullInfoDiagram.styles';

const FullInfoDiagram = () => {
  const classes = useStyles();

  return <Box>DIAGRAM</Box>;
};

export default FullInfoDiagram;
